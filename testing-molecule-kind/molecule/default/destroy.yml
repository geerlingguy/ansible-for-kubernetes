---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    kubeconfig: ~/.kube/config-molecule-test

  tasks:
    - name: Delete the kind cluster.
      command: >-
        kind delete cluster
        --name molecule-test
        --kubeconfig {{ kubeconfig }}
      changed_when: false
